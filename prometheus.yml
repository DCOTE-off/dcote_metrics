global:
    scrape_interval: 5s

scrape_configs:
    - job_name: "backend"
      static_configs:
          - targets: ["backend:3000"]
      authorization:
          credentials: "SUPERSECRETPASSWORD"
    - job_name: "node"
      static_configs:
          - targets: ["node_exporter:9100"]
